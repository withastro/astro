---
import Markdown from '@astrojs/markdown-component';

const outer = `# Outer`;
const inner = `## Inner`;
---

<style>
	#root { color: green; }
</style>
<div id="root">
	<Markdown content={outer} />

	<Markdown  is:raw>
		# Nested

		<Markdown content={inner} />
	</Markdown>
</div>
