---
import { FOO, SECRET } from "astro:env/server";
import { BAR } from "astro:env/client";

const foo = FOO;
const bar = BAR;
const secret = SECRET;
---

<h2>{foo}</h2>
<h2>{bar}</h2>
<h2>{secret}</h2>

<script>
	import { BAR } from "astro:env/client";
	
	console.log(BAR);
</script>
