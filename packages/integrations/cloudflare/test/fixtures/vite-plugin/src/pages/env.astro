---
import { FOO, SECRET } from "astro:env/server";
import { BAR } from "astro:env/client";

const foo = FOO;
const bar = BAR;
const secret = SECRET;
---

<h1>{foo}</h1>
<h1>{bar}</h1>
<h1>{secret}</h1>

<script>
	import { BAR } from "astro:env/client";
	
	console.log(BAR);
</script>
