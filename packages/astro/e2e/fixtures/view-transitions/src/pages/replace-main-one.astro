---
import Layout from '../components/Layout.astro';
---
<Layout>
	<section>
		<p id="name">Replace Main Section</p>
		<p>This is the main section</p>
	</section>
	<a id="click" href="/keep-two">go to next page</a>
</Layout>
<script>
	import { builtInSwap } from "../../node_modules/astro/dist/transitions/swap-steps"
	builtInSwap.extend({
			swapBodyElement(newElement: Element, oldElement: Element, previousSwapBodyElement) {
				const newMain = newElement.querySelector('main section');
				const oldMain = oldElement.querySelector('main section');
				if (newMain && oldMain) {
					previousSwapBodyElement(newMain, oldMain);
				} else {
					previousSwapBodyElement(newElement, oldElement);
				}
			}
		})

	document.addEventListener('astro:before-swap', (e) => {
		e.swap = () => builtInSwap.extended(e.newDocument)
	});
</script>
