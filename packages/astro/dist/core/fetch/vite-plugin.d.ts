import { type Plugin as VitePlugin } from 'vite';
import type { AstroSettings } from '../../types/astro.js';
export declare function vitePluginFetchable({ settings }: { settings: AstroSettings }): VitePlugin;
