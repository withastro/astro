---
import ConditionalSlottedCallback from '../../components/slots/ConditionalSlottedCallback.astro';
---

<ConditionalSlottedCallback>
	{
		true && (
			<fragment slot="block">
				{({ test }) => <div id="conditional-block">Block: {test}</div>}
			</fragment>
		)
	}
</ConditionalSlottedCallback>
