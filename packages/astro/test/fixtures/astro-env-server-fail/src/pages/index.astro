<script>
    import { FOO } from "astro:env/server"

    if (Math.random() * 2 > 10) {
        // We need to use this FOO variable to prevent tree shaking from removing the import.
        console.log("This will never happen", FOO)
    }
</script>
