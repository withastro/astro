---
export const getStaticPaths = async () => {
  return ['en-us', 'es-mx', 'fr-fr'].map((locale) => ({
    params: {
      locale,
    },
  }))
}

const currentLocale = Astro.currentLocale;
---
<html>
<head>
  <title>Astro</title>
</head>
<body>
currentLocale: {currentLocale}
</body>
</html>