---
import { Markdown } from 'astro/components';

const outer = `# Outer`;
const inner = `## Inner`;
---

<div>
  <Markdown content={outer} />

  <Markdown>
    # Nested

    <Markdown content={inner} />
  </Markdown>
</div>