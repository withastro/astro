---
const isPrerendered = JSON.stringify(Astro.isPrerendered);
const localsPrerender = JSON.stringify(Astro.locals.localsPrerender);
---

<p id="prerender">Astro route prerender: {isPrerendered}</p>
<p id="prerender-middleware">Astro route prerender middleware: {localsPrerender}</p>
