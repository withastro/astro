---
import { getCollection, render } from "astro:content";
const posts = await getCollection("blog");
const mdxPost = posts.find(p => p.id === 'mdx-with-css');
const { Content } = await render(mdxPost);
---

<html>
<head>
	<title>MDX Blog</title>
</head>
<body>
	<Content />
</body>
</html>
