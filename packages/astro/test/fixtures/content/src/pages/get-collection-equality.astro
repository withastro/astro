---
import { getCollection } from 'astro:content';

const blog1 = await getCollection('blog');
const blog2 = await getCollection('blog');
---
<html>
<head>
	<title>Index</title>
</head>
<body>
	<h1>Are two collection instances different objects?</h1>
	<p data-are-equal>{JSON.stringify(blog1 === blog2)}</p>
</body>
</html>
