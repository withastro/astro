---
import { getEntry, render } from 'astro:content';

const entry = await getEntry('blog', 'promo/4-launch-week');

const { Content } = await render(entry);
---
<html>
<head>
	<title>Launch Week</title>
</head>
<body>
	<Content />
</body>
</html>
