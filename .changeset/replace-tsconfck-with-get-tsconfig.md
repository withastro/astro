---
'astro': patch
'astro-scripts': patch
---

Replaces the discontinued `tsconfck` dependency with `get-tsconfig` and `jsonc-parser`. This is an internal infrastructure change with no public API change.
