---
'astro': minor
---

Adds a `--timings` flag to `astro build` that prints a summary of where the build spent its time: build phases, Vite plugins, Markdown and syntax highlighting, page rendering, images and integrations.
