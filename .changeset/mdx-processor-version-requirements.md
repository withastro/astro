---
'@astrojs/mdx': major
---

Rendering `.mdx` files now requires `@astrojs/markdown-satteri` 0.4.0 or later, and `@astrojs/markdown-remark` 7.3.0 or later if you use `unified()`. Update whichever appears in your own `package.json` — a `^0.3.x` or `^7.2.x` range will not move on its own.
