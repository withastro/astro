---
'astro': minor
---

Adds optional `placement` config option for the dev toolbar. Configure the default toolbar position (`'bottom-left'`, `'bottom-center'`, or `'bottom-right'`) via `devToolbar.placement` in your Astro config. User preferences from the toolbar UI (stored in localStorage) still take priority.
