---
'astro': patch
'@astrojs/language-server': patch
---

Fixes TypeScript type checking for Vue components imported from pre-built libraries with `.d.ts` type declarations generated by `vite-plugin-dts`. Previously, using such components in `.astro` files would produce a false `IntrinsicAttributes` error when passing props.
