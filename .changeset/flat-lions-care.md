---
'@astrojs/cloudflare': minor
---

Adds support for `astro preview`

Developers can now use `astro preview` after running a build to preview their Cloudflare Workers applications using workerd, matching the production environment.
