---
'astro': patch
'@astrojs/rss': patch
'@astrojs/sitemap': patch
---

Updates the bundled Zod version to 4.5.

String `.min()` / `.max()` / `.length()` count Unicode code points. `z.iso.datetime()` requires seconds.

[Zod 4.5 release post](https://zod.dev/blog/zod-4-5)
