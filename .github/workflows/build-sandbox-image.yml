name: Build Sandbox Image

on:
  push:
    paths: ['.flue/Dockerfile.sandbox']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - uses: docker/build-push-action@v6
        with:
          file: .flue/Dockerfile.sandbox
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/flue-sandbox:latest
            ghcr.io/${{ github.repository }}/flue-sandbox:${{ hashFiles('.flue/Dockerfile.sandbox') }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
