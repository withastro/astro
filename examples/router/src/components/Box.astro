---
const { color = "red" } = Astro.props;
---

<div class="box" />

<style define:vars={{ color }}>
	.box {
		--size: 400px;
		width: var(--size);
		height: var(--size);
		background: var(--color);
	}
</style>
