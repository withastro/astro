---
const { color = "red" } = Astro.props;
---

<div class="box" />

<style define:vars={{ color }}>
div {
	display: block;
	--size: 256px;
	width: var(--size);
	height: var(--size);
	background: var(--color);
}
</style>
