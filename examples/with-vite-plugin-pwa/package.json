{
  "name": "@example/with-vite-plugin-pwa",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "cross-env SOURCE_MAP=true SW_DEV=true astro dev",
    "start-sw": "cross-env SOURCE_MAP=true SW=true SW_DEV=true astro dev",
    "build": "cross-env SOURCE_MAP=true astro build",
    "build-sw": "cross-env SOURCE_MAP=true SW=true astro build",
    "preview": "npm run build && astro preview",
    "preview-sw": "npm run build-sw && astro preview"
  },
  "devDependencies": {
    "@astrojs/pwa": "^0.0.0",
    "astro": "^1.0.0-beta.65",
    "cross-env": "^7.0.3",
    "workbox-window": "^6.5.3"
  }
}
